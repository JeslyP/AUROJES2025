// Use this devcontainer.json to develop within a standard Docker environment,
// with VNC providing the GUI support.
// It uses the prebuilt image from the GitHub Container Registry,
// so does not require a Dockerfile.
{
  "name": "auro-vnc",
  "image": "ghcr.io/uoy-robostar/ros2-tb3/auro-dev:latest",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/${localWorkspaceFolderBasename},type=bind",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "mounts": [
    "source=${localEnv:HOME}${localEnv:USERPROFILE},target=/home/ubuntu/homedir,type=bind"
  ],
  "remoteUser": "ubuntu",
  "portsAttributes": {
      "6080": {
          "label": "Desktop"
      }
  },
  "postStartCommand": "touch /workspaces/.bash_history && ln -sf /workspaces/.bash_history /home/ubuntu/.bash_history ; /workspaces/${localWorkspaceFolderBasename}/.devcontainer/gitconfig.sh",
  "remoteEnv": {
    "USERNAME": "${localEnv:USERNAME}",
    "USERDNSDOMAIN": "${localEnv:USERDNSDOMAIN}"
  },
  "containerEnv": {
    "VNC_RESOLUTION": "1280x720x24"
  },
  "customizations": {
    "auro": {
      "scenarios": [
        {
          "name": "3 Robots (GUI)",
          "description": "Three robots, Gazebo GUI enabled, RViz disabled.",
          "parameters": {
            "num_robots": 3,
            "use_rviz": "false",
            "barrels": "true",
            "wait_for_barrels": "true",
            "headless": "false",
            "limit_real_time_factor": "true"
          }
        },
        {
          "name": "3 Robots (Headless)",
          "description": "Three robots, headless, runs faster than real-time, RViz disabled.",
          "parameters": {
            "num_robots": 3,
            "use_rviz": "false",
            "barrels": "true",
            "wait_for_barrels": "true",
            "headless": "true",
            "limit_real_time_factor": "false"
          }
        },
        {
          "name": "1 Robot (GUI)",
          "description": "Single robot, Gazebo GUI enabled, RViz disabled.",
          "parameters": {
            "num_robots": 1,
            "use_rviz": "false",
            "barrels": "true",
            "wait_for_barrels": "true",
            "headless": "false",
            "limit_real_time_factor": "true"
          }
        }
      ]
    }
  }
}